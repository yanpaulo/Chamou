@model IEnumerable<Chamou.Web.Models.Entities.Place>

@{
    ViewBag.Title = "Index";
}

<h2>Lugares</h2>

<p>
    @Html.ActionLink("Cadastrar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Detalhes", "Details", new { id=item.Id }) |
            @Html.ActionLink("Excluir Socialmente", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
